var data = "<%= escape_javascript( render( :partial => "weaves/weaves", :locals => {:weaves => @weaves} ) )%>";
$(".endless").append(data);
<% if @per_page and !(@weaves.size < @per_page) %>
checkScroll();
<% end %>
make_images_zoomable();
